#include <stdio.h>
#include <stdin.h>
#include <string.h>

int main () {
   char commando[20];
   char go;
  
    while (go == 'g') {
   fgets(commando, sizeof(commando), stdin);
  
      if (commando === "nopsql") {

      }
      
     system() // like system("cls");  
   
     scanf("% ^[\n], &go); 
  }
  
  return 0;
}
